# 📤 Инструкция по загрузке на GitHub

## Шаг 1: Инициализация Git (если еще не инициализирован)

```powershell
cd d:\DevTools\Database\UspMusicFinder
git init
```

## Шаг 2: Добавить все файлы

```powershell
git add .
```

## Шаг 3: Создать первый коммит

```powershell
git commit -m "Initial commit: UspMusicFinder Bot MVP (Day 1-4)"
```

## Шаг 4: Создать репозиторий на GitHub

1. Перейди на <https://github.com/new>
2. Введи имя репозитория: `UspMusicFinder`
3. Описание: `Telegram бот для поиска и скачивания музыки с YouTube`
4. Выбери: Public (если хочешь общего доступа)
5. Нажми "Create repository"

## Шаг 5: Добавить удаленный репозиторий

```powershell
git remote add origin https://github.com/yourusername/UspMusicFinder.git
git branch -M main
```

Замени `yourusername` на твое имя пользователя GitHub!

## Шаг 6: Первая загрузка

```powershell
git push -u origin main
```

## Шаг 7: Проверить результат

Открой <https://github.com/yourusername/UspMusicFinder>

---

## 📋 Список файлов для загрузки

✅ Основные файлы:

- src/ (весь код)
- logs/ (директория для логов)
- data/ (директория для данных)
- .env (конфигурация - не коммитить!)
- .gitignore
- requirements.txt
- README.md
- DEVELOPMENT_PLAN.md
- PROGRESS_DAY1-4.md

❌ НЕ коммитить:

- __pycache__/ (в .gitignore)
- .venv/ (в .gitignore)
- *.log (в .gitignore)
- .env (в .gitignore для безопасности)

---

## 🔄 Последующие обновления

```powershell
# После изменений
git add .
git commit -m "Описание изменений"
git push origin main
```

---

## 🏷️ Создание Release (опционально)

```powershell
# После успешной загрузки
git tag -a v0.1.0 -m "Day 1-4: MVP ready"
git push origin v0.1.0
```

---

## ✅ Проверка статуса

```powershell
# Посмотреть статус
git status

# Посмотреть логи коммитов
git log --oneline

# Посмотреть удаленные репозитории
git remote -v
```

---

## 🔐 Безопасность .env

Убедись что `.env` в `.gitignore` чтобы не закоммитить токен!

```bash
# Проверить
git status

# Если .env был добавлен случайно
git rm --cached .env
git commit -m "Remove .env from tracking"
```

---

__После выполнения всех шагов, твой репозиторий будет на GitHub!__ 🚀
