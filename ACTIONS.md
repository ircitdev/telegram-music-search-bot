# 🚀 UspMusicFinder - Начало разработки

## ✅ Что уже готово

- ✅ Проект создан: `D:\DevTools\Database\UspMusicFinder`
- ✅ Структура директорий создана
- ✅ Виртуальное окружение `.venv` готово
- ✅ Документация готова:
  - [PROJECT_SPEC.md](PROJECT_SPEC.md) - техническая спецификация (полная)
  - [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) - план разработки (4 недели)
- ✅ `.env` файл создан с токеном бота
- ✅ `requirements.txt` обновлен с нужными пакетами
- ✅ Claude Code slash команды готовы

---

## 📋 Ваши действия СЕЙЧАС

### Шаг 1: Откройте проект в VS Code

```powershell
cd D:\DevTools\Database\UspMusicFinder
code .
```

### Шаг 2: Активируйте виртуальное окружение

**В терминале VS Code (PowerShell):**

```powershell
.\.venv\Scripts\activate.ps1
```

Вы должны увидеть `(.venv)` в начале строки терминала.

### Шаг 3: Установите зависимости

```powershell
pip install -r requirements.txt
```

Это установит:
- `aiogram` - Telegram Bot framework
- `yt-dlp` - универсальный загрузчик музыки
- `mutagen` - работа с метаданными аудио
- `aiohttp`, `aiofiles` - асинхронная работа
- `python-dotenv` - работа с .env
- `aiosqlite` - база данных для статистики

### Шаг 4: Проверьте .env файл

Откройте [.env](.env) и убедитесь что токен на месте:

```env
BOT_TOKEN=8409655187:AAHzhBh8F9KuMiAyemy-IVlF3yNgXXpPI6E
BOT_USERNAME=UspMusicFinder_bot
```

---

## 🤖 Начните разработку с Claude Code

### ⭐ РЕКОМЕНДУЕМЫЙ СПОСОБ: Build with Agent

1. **Откройте [README.md](README.md)**
2. **Нажмите кнопку "Build with Agent"** (правый верхний угол)
3. **Введите задачу:**

```
Создай базовый музыкальный Telegram бот согласно DEVELOPMENT_PLAN.md (День 1-2)

Задачи:
1. Создать src/config.py - загрузка настроек из .env (Settings с pydantic)
2. Создать src/utils/logger.py - настройка логирования (в консоль и файл)
3. Создать src/bot.py - инициализация Bot и Dispatcher (aiogram 3.x)
4. Создать src/models.py - класс Track (dataclass)
5. Создать src/handlers/start.py - /start и /help команды
6. Обновить src/main.py - точка входа с включением роутеров

Требования:
- Использовать aiogram 3.15.0
- Async/await стиль
- Загрузка настроек из .env через pydantic-settings
- Логирование в консоль и logs/bot.log
- /start должен показывать инструкцию по использованию
- /help должен показывать список команд

После создания - запусти бот и протестируй команды /start и /help
```

4. **Claude создаст все файлы и запустит бот**

---

### Альтернатива: Используйте Slash команды

В Claude Code (Ctrl+L):

```
/analyze DEVELOPMENT_PLAN.md
```

Затем:

```
Создай файлы для базового бота (День 1-2 плана)
```

---

## 🎵 Что будет создано (День 1-2)

### Файлы:

**1. src/config.py**
- Класс Settings с параметрами из .env
- BOT_TOKEN, пути к директориям, лимиты

**2. src/utils/logger.py**
- Настройка логирования
- Запись в `logs/bot.log` и консоль

**3. src/bot.py**
- Инициализация Bot и Dispatcher
- Конфигурация aiogram

**4. src/models.py**
- Dataclass Track (id, title, artist, duration, url)
- Метод formatted_duration

**5. src/handlers/start.py**
- `/start` команда - приветствие и инструкция
- `/help` команда - список команд и описание

**6. src/main.py**
- Точка входа приложения
- Подключение роутеров
- Запуск polling

---

## 🧪 Тестирование

После того как Claude создаст файлы:

### 1. Запустите бота

```powershell
python src/main.py
```

Вы должны увидеть:

```
[INFO] Bot started successfully
[INFO] Bot @UspMusicFinder_bot is running...
```

### 2. Протестируйте в Telegram

Откройте Telegram и найдите: **@UspMusicFinder_bot**

Отправьте команды:
- `/start` - должен показать приветствие и инструкцию
- `/help` - должен показать справку

### 3. Остановите бота

Нажмите `Ctrl+C` в терминале

---

## 📖 Следующие шаги (после базового бота)

Следуйте плану в [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md):

**День 3-4:** YouTube Music поиск
- Интеграция yt-dlp
- Функция `search_music()`
- Парсинг результатов

**День 5-7:** Скачивание и отправка
- Функция `download_track()`
- Конвертация в MP3
- Метаданные (mutagen)
- Отправка в Telegram

**День 8-10:** Inline keyboard
- Кнопки 1-10
- Callback handlers
- Кэширование результатов

И далее по плану...

---

## 🔧 Полезные команды

### Установка dev зависимостей

```powershell
pip install -r requirements-dev.txt
```

Это добавит:
- `pytest` - тестирование
- `black` - форматирование
- `flake8` - линтинг
- `mypy` - проверка типов

### Запуск тестов

```powershell
pytest
```

### Форматирование кода

```powershell
black src tests
```

### Проверка кода

```powershell
flake8 src tests
mypy src
```

---

## 📚 Документация

- [PROJECT_SPEC.md](PROJECT_SPEC.md) - полная техническая спецификация (73KB)
  - Архитектура бота
  - Технический стек (aiogram, yt-dlp, mutagen)
  - Поисковые API (YouTube Music, iTunes, Deezer)
  - Структура проекта
  - Примеры кода

- [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) - план на 4 недели (28 дней)
  - Неделя 1: MVP - базовый поиск и скачивание
  - Неделя 2: UI/UX - кнопки и TOP популярных
  - Неделя 3: Inline режим и расширения
  - Неделя 4: Production - Docker, деплой, мониторинг

---

## ❓ Проблемы?

### Виртуальное окружение не активируется

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Пакеты не устанавливаются

Проверьте подключение к интернету и обновите pip:

```powershell
python -m pip install --upgrade pip
```

### Бот не запускается

1. Проверьте `.env` файл - токен должен быть на месте
2. Проверьте логи в `logs/bot.log`
3. Убедитесь что все пакеты установлены: `pip list`

### ffmpeg не установлен (для yt-dlp)

**Windows:**
```powershell
choco install ffmpeg
```

Или скачайте с https://ffmpeg.org/download.html

**Linux:**
```bash
sudo apt-get install ffmpeg
```

---

## 🎯 Краткая версия (TL;DR)

```powershell
# 1. Откройте проект
cd D:\DevTools\Database\UspMusicFinder
code .

# 2. Активируйте venv
.\.venv\Scripts\activate.ps1

# 3. Установите пакеты
pip install -r requirements.txt

# 4. Установите ffmpeg (если нет)
choco install ffmpeg

# 5. Используйте Build with Agent в README.md
# Или попросите Claude Code создать базовый бот

# 6. Запустите
python src/main.py

# 7. Тестируйте в Telegram: @UspMusicFinder_bot
```

---

## 🎨 Особенности проекта

### Бот умеет (после полной разработки):

✅ **Поиск музыки** - по названию или исполнителю
✅ **Скачивание MP3** - через YouTube Music (yt-dlp)
✅ **Inline режим** - @UspMusicFinder_bot в любом чате
✅ **TOP популярных** - топ песен по странам
✅ **Метаданные** - правильные теги ID3 (исполнитель, название)
✅ **Rate limiting** - 5 запросов в минуту
✅ **Кэширование** - результаты на 10 минут
✅ **Статистика** - популярные запросы и треки

### Технологии:

- **aiogram 3.15** - современный async Telegram Bot framework
- **yt-dlp** - универсальный загрузчик (YouTube, SoundCloud, и др.)
- **mutagen** - работа с метаданными MP3
- **SQLite** - статистика использования
- **Docker** - контейнеризация для деплоя

---

## 📝 Примечания

### YouTube Music как основной источник

Бот использует YouTube Music через yt-dlp потому что:
- ✅ Бесплатный API (не требует ключей)
- ✅ Огромная библиотека треков
- ✅ Высокое качество (192 kbps MP3)
- ✅ Надежность и стабильность
- ✅ Поддержка поиска на разных языках

### Альтернативные источники (опционально)

В спецификации описаны интеграции:
- iTunes Search API (preview 30 сек)
- Deezer API (поиск)
- Last.fm API (топ треки)
- VK Music (неофициальный API)

---

**Готовы начать? Откройте VS Code и используйте Build with Agent!** 🚀
