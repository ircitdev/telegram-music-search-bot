# 🤖 CLAUDE.md - Инструкции для AI-ассистента

Этот файл содержит контекст и инструкции для работы с проектом UspMusicFinder Bot.

---

## 📋 Обзор проекта

**UspMusicFinder Bot** - Telegram бот для поиска и скачивания музыки с YouTube Music.

- **Бот:** [@UspMusicFinder_bot](https://t.me/UspMusicFinder_bot)
- **Канал:** [@TopMusicToday](https://t.me/TopMusicToday)
- **Сервер:** `root@31.44.7.144`
- **Путь на сервере:** `/root/uspmusic-bot`
- **Сервис:** `uspmusic.service`
- **GitHub:** [telegram-music-search-bot](https://github.com/ircitdev/telegram-music-search-bot)

---

## 🛠️ Технологический стек

### Backend
- **Python 3.11+**
- **aiogram 3.15** - Telegram Bot framework
- **yt-dlp** - загрузчик музыки с YouTube
- **mutagen** - работа с метаданными аудио
- **aiohttp** - асинхронные HTTP запросы
- **aiosqlite** - асинхронная работа с SQLite

### Платежи
- **Telegram Stars** - встроенные платежи Telegram
- **CryptoBot API** - криптовалютные платежи (USDT, TON, BTC, ETH)

### Инфраструктура
- **Docker** - контейнеризация
- **GitHub Actions** - CI/CD
- **Sentry** - мониторинг ошибок
- **systemd** - управление сервисом на сервере

---

## 📁 Структура проекта

```
UspMusicFinder/
├── src/
│   ├── bot.py              # Инициализация бота
│   ├── config.py           # Настройки из .env
│   ├── main.py             # Точка входа
│   ├── models.py           # Модель Track
│   ├── keyboards.py        # Inline клавиатуры
│   │
│   ├── handlers/           # Обработчики команд
│   │   ├── start.py        # /start, /help
│   │   ├── callbacks.py    # Обработка inline-кнопок
│   │   ├── admin.py        # Админ-панель
│   │   ├── premium.py      # Премиум и платежи
│   │   ├── recognize.py    # Распознавание музыки
│   │   ├── top.py          # ТОП треков
│   │   ├── history.py      # История скачиваний
│   │   ├── favorites.py    # Избранное
│   │   └── ...
│   │
│   ├── locales/            # Мультиязычность (RU/EN/UZ)
│   ├── database/           # SQLite + репозитории
│   ├── payments/           # Stars, CryptoBot, YooMoney
│   ├── searchers/          # Поиск по YouTube
│   ├── downloaders/        # Скачивание через yt-dlp
│   └── utils/              # Утилиты (logger, cache, cleanup)
│
├── dashboard/              # Web-админка
├── tests/                  # Тесты pytest
├── scripts/                # Backup/restore скрипты
├── .env                    # Конфигурация (НЕ в Git!)
└── requirements.txt        # Python зависимости
```

---

## 🔑 Важные файлы

### Конфигурация (.env)
```env
# Telegram
BOT_TOKEN=8409655187:AAHzhBh8F9KuMiAyemy-IVlF3yNgXXpPI6E
BOT_USERNAME=UspMusicFinder_bot
ADMIN_IDS="65876198"  # ID админов через запятую

# Лимиты
MAX_FILE_SIZE=52428800  # 50MB
FREE_DAILY_LIMIT=10     # Лимит бесплатных скачиваний/день

# API (опционально)
AUDD_API_KEY=           # Распознавание музыки
CRYPTOBOT_TOKEN=        # Криптоплатежи
```

### База данных
- **Путь:** `./data/database.db`
- **Тип:** SQLite (асинхронный через aiosqlite)
- **Таблицы:**
  - `users` - пользователи (премиум, бонусы, язык)
  - `downloads` - история скачиваний
  - `favorites` - избранные треки
  - `track_stats` - статистика треков
  - `daily_downloads` - дневные лимиты
  - `payments` - платежи
  - `referrals` - рефералы

---

## 🚀 Развертывание и обновление

### Локальная разработка

```bash
# Активировать venv
.\.venv\Scripts\activate  # Windows
source .venv/bin/activate # Linux

# Установить зависимости
pip install -r requirements.txt

# Запустить бота
python -m src.main
```

### Обновление на сервере (31.44.7.144)

**ВАЖНО:** Всегда следуй этой последовательности:

```bash
# 1. Закоммитить изменения локально
git add <файлы>
git commit -m "Описание изменений

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
git push origin master

# 2. Подключиться к серверу
ssh root@31.44.7.144

# 3. Обновить код на сервере
cd /root/uspmusic-bot
git stash  # Если есть локальные изменения
git pull origin master

# 4. Установить зависимости (если обновлялись)
pip3 install -r requirements.txt

# 5. Перезапустить сервис
systemctl restart uspmusic

# 6. Проверить статус
systemctl status uspmusic
journalctl -u uspmusic -n 20 --no-pager

# 7. Убедиться, что бот запущен
ps aux | grep "python.*main.py"
```

### Типичные проблемы

**Проблема:** Бот падает с `ModuleNotFoundError`
```bash
# Решение: установить зависимости
pip3 install -r requirements.txt
systemctl restart uspmusic
```

**Проблема:** Git конфликты при pull
```bash
# Решение: сохранить изменения и обновиться
git stash
git pull origin master
# Если нужно восстановить: git stash pop
```

**Проблема:** Сервис не запускается
```bash
# Посмотреть логи ошибок
journalctl -u uspmusic -n 50 --no-pager
# Или запустить вручную для отладки
cd /root/uspmusic-bot
python3 -m src.main
```

---

## 👨‍💼 Админ-функции

### Админ ID
Текущий админ: `65876198` (указан в `.env`)

Для добавления админов:
```env
ADMIN_IDS="65876198,123456789,987654321"
```

### Админ-команды

| Команда | Описание |
|---------|----------|
| `/admin` | 🎛️ Интерактивная панель с кнопками |
| `/stats` | 📊 Статистика бота |
| `/users` | 👥 Количество пользователей |
| `/top` | 🏆 ТОП-10 пользователей |
| `/user_stats ID` | 👤 Статистика конкретного пользователя |
| `/setpremium ID дни` | ⭐ Выдать/снять премиум |
| `/mailing` | 📢 Массовая рассылка |
| `/web_admin` | 🌐 Web-дашборд с кодом доступа |
| `/post_top` | 📣 Опубликовать топ в канал |
| `/update_ytdlp` | 🔧 Обновить yt-dlp |

### Интерактивная админ-панель

Вызов `/admin` показывает меню с кнопками:
- 📊 Статистика
- 👥 Пользователи
- 🏆 ТОП-10
- 🌐 Web-панель
- 📢 Рассылка
- 📣 Пост в канал
- 🔧 Обновить yt-dlp
- 📝 Справка

Каждая кнопка имеет callback-обработчик в `src/handlers/admin.py`.

---

## 🐛 Обработка ошибок

### Система диагностики ошибок скачивания

**Файл:** `src/downloaders/youtube_dl.py`

При ошибке скачивания определяется тип проблемы:
- ❌ **Video unavailable** - видео удалено/недоступно
- 🔒 **Private video** - приватное видео
- ©️ **Copyright** - авторские права
- 🌍 **Geo-restricted** - региональная блокировка
- 📦 **Too large** - превышен размер (>50MB)

**Файл:** `src/handlers/callbacks.py` (строки 341-405)

Пользователь видит информативное сообщение с:
- Названием трека и исполнителем
- Конкретной причиной ошибки
- Список треков для выбора другого
- Подсказку о новом поиске

---

## 📝 Стандарты кода

### Commit сообщения

Формат:
```
Тип: Краткое описание

- Детальное описание изменения 1
- Детальное описание изменения 2

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
```

Типы:
- `Feature:` - новая функциональность
- `Fix:` - исправление бага
- `Refactor:` - рефакторинг
- `Docs:` - документация
- `Test:` - тесты

### Python код

- **Стиль:** PEP 8
- **Async/await:** Всегда для I/O операций
- **Логирование:** Использовать `src.utils.logger`
- **Обработка ошибок:** try/except с логированием

Пример:
```python
from src.utils.logger import logger

async def some_function():
    try:
        result = await some_async_operation()
        logger.info(f"Operation successful: {result}")
        return result
    except Exception as e:
        logger.error(f"Operation failed: {e}", exc_info=True)
        raise
```

---

## 🔒 Безопасность

### Важные правила

1. **Никогда не коммитить:**
   - `.env` файл с токенами
   - `data/database.db` с данными пользователей
   - Логи с личной информацией

2. **API токены:**
   - Хранить только в `.env`
   - Не логировать токены

3. **Git операции:**
   - НИКОГДА не использовать `git push --force` на master
   - НИКОГДА не использовать `git reset --hard` без подтверждения
   - Всегда проверять `git status` перед коммитом

4. **Сервер:**
   - Не удалять файлы базы данных
   - Делать backup перед критическими изменениями
   - Проверять логи после deployment

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
pytest

# С покрытием
pytest --cov=src

# Verbose
pytest -v
```

### Критические тесты перед deployment

1. **Импорт модулей:**
   ```bash
   python3 -c "from src.main import main; print('OK')"
   ```

2. **Конфигурация:**
   ```bash
   python3 -c "from src.config import settings; print(settings.BOT_USERNAME)"
   ```

3. **База данных:**
   ```bash
   pytest tests/test_database.py -v
   ```

---

## 📊 Мониторинг

### Логи

Локально:
```bash
tail -f logs/bot.log
```

На сервере:
```bash
journalctl -u uspmusic -f
journalctl -u uspmusic -n 50 --no-pager
journalctl -u uspmusic --since "1 hour ago"
```

### Метрики

Проверить статус сервиса:
```bash
systemctl status uspmusic
systemctl is-active uspmusic
```

Использование ресурсов:
```bash
ps aux | grep python
top -p $(pgrep -f "python.*main.py")
```

---

## 🔄 Чеклист для крупных изменений

Перед deployment выполни:

- [ ] Локально протестировать изменения
- [ ] Запустить pytest
- [ ] Проверить что нет секретов в коде
- [ ] Обновить README.md если нужно
- [ ] Обновить CHANGELOG.md
- [ ] Закоммитить с понятным сообщением
- [ ] Push в GitHub
- [ ] На сервере: `git pull`
- [ ] На сервере: `pip3 install -r requirements.txt`
- [ ] На сервере: `systemctl restart uspmusic`
- [ ] Проверить логи: `journalctl -u uspmusic -n 20`
- [ ] Проверить статус: `systemctl status uspmusic`
- [ ] Протестировать бота в Telegram

---

## 🆘 Критические действия

### Откат к предыдущей версии

```bash
# На сервере
cd /root/uspmusic-bot
git log --oneline -5  # Посмотреть коммиты
git reset --hard <commit-hash>
systemctl restart uspmusic
```

### Восстановление из backup

```bash
cd /root/uspmusic-bot
./scripts/restore.sh backup_YYYYMMDD_HHMMSS.db
```

### Экстренная остановка

```bash
systemctl stop uspmusic
```

---

## 📞 Контакты и ресурсы

- **Бот:** [@UspMusicFinder_bot](https://t.me/UspMusicFinder_bot)
- **Канал:** [@TopMusicToday](https://t.me/TopMusicToday)
- **GitHub:** [telegram-music-search-bot](https://github.com/ircitdev/telegram-music-search-bot)
- **Сервер:** 31.44.7.144
- **Админ ID:** 65876198

### Документация зависимостей

- [aiogram docs](https://docs.aiogram.dev/)
- [yt-dlp docs](https://github.com/yt-dlp/yt-dlp#readme)
- [Python asyncio](https://docs.python.org/3/library/asyncio.html)

---

## ⚡ Быстрые команды

```bash
# Локальная разработка
python -m src.main

# Git workflow
git add . && git commit -m "message" && git push

# Деплой на сервер (полный цикл)
ssh root@31.44.7.144 "cd /root/uspmusic-bot && git pull && pip3 install -r requirements.txt && systemctl restart uspmusic && systemctl status uspmusic"

# Просмотр логов на сервере
ssh root@31.44.7.144 "journalctl -u uspmusic -f"

# Проверка статуса
ssh root@31.44.7.144 "systemctl is-active uspmusic && echo 'Bot is running' || echo 'Bot is down'"
```

---

**Последнее обновление:** 2026-02-22
**Версия бота:** 1.0.0
**Статус:** ✅ Production Ready
