# UspMusicFinder Bot - Техническая спецификация

## 📋 Информация о проекте

- **Название бота:** @UspMusicFinder_bot
- **API Token:** `8409655187:AAHzhBh8F9KuMiAyemy-IVlF3yNgXXpPI6E`
- **Назначение:** Telegram бот для поиска и скачивания музыки
- **Аналоги:** @fullmp3bot, другие музыкальные боты в Telegram

---

## 🎯 Основная функциональность

### Что делает бот:

1. **Поиск музыки** по названию песни или исполнителю
2. **Показывает результаты** в виде списка с кнопками
3. **Скачивает и отправляет** аудио файлы в Telegram
4. **Поддержка популярных треков** (TOP популярных песен)
5. **Inline режим** - поиск прямо из любого чата

---

## 🎨 Пользовательский сценарий (на основе скриншотов)

### Сценарий 1: Поиск по тексту

1. Пользователь отправляет `/start`
2. Бот показывает приветствие и инструкцию:
   ```
   Привет! Чтобы я смог распознать музыку, отправь мне что-то из этого:

   • Название песни или исполнителя
   • Слова из песни
   • Голосовое сообщение
   • Видео
   • Аудио
   • Видеосообщение

   (Бот работает напрямую и в чатах тоже.)
   ```

3. Пользователь вводит название (например: "Время назад")
4. Бот показывает результаты поиска с кнопками выбора:
   ```
   Время назад

   1. Время, назад! 4:13
   2. Сплин «Время, назад» 4:27
   3. Из фильма Егерь песня Сплин Время назад .avi 2:18
   ...
   ```
5. Пользователь нажимает кнопку с номером (1-10)
6. Бот показывает "Загрузка..."
7. Бот отправляет аудио файл

### Сценарий 2: TOP популярных песен

1. Пользователь отправляет `/top` или "top"
2. Бот показывает список популярных песен с кнопками выбора стран
3. Пользователь выбирает страну (флаги)
4. Бот показывает ТОП песен для этой страны
5. Пользователь нажимает на песню
6. Бот отправляет аудио

### Сценарий 3: Inline режим

1. Пользователь в любом чате вводит: `@UspMusicFinder_bot Название песни`
2. Бот показывает результаты inline
3. Пользователь выбирает трек
4. Трек отправляется в чат

---

## 🏗️ Архитектура

```
┌─────────────────┐
│  Telegram Bot   │
│   (aiogram)     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Dispatcher    │ ← Обработка команд и сообщений
└────────┬────────┘
         │
         ├──────────────────┐
         │                  │
         ▼                  ▼
┌─────────────────┐  ┌─────────────────┐
│  Text Handler   │  │  Inline Handler │
│  (поиск текст)  │  │  (inline query) │
└────────┬────────┘  └────────┬────────┘
         │                    │
         └──────────┬─────────┘
                    ▼
         ┌─────────────────┐
         │  Music Searcher │ ← Поиск треков
         └────────┬────────┘
                  │
                  ▼
         ┌─────────────────┐
         │   Downloader    │ ← Скачивание MP3
         └────────┬────────┘
                  │
                  ▼
         ┌─────────────────┐
         │  Telegram Send  │ ← Отправка файла
         └─────────────────┘
```

---

## 🔧 Технический стек

### Backend

- **Python 3.11+**
- **aiogram 3.15.0** - Telegram Bot framework
- **aiohttp** - HTTP клиент для API запросов
- **yt-dlp** - универсальный загрузчик (для YouTube Music и др.)
- **mutagen** - работа с метаданными аудио (теги ID3)

### Поисковые API и источники

**Варианты (будем тестировать):**

1. **YouTube Music API** (через yt-dlp)
   - Поиск: `ytsearch10:QUERY`
   - Скачивание: yt-dlp с параметром `-x --audio-format mp3`

2. **iTunes Search API** (бесплатный)
   - API: `https://itunes.apple.com/search?term=QUERY&media=music&limit=10`
   - Скачивание: preview URL (30 сек) или ищем полную версию

3. **Deezer API** (бесплатный для поиска)
   - API: `https://api.deezer.com/search?q=QUERY&limit=10`
   - Скачивание: через сторонние источники

4. **Spotify API** (требует регистрацию)
   - Поиск треков
   - Получение метаданных

5. **VK Music API** (неофициальный)
   - vk_api библиотека
   - Поиск и скачивание

**Рекомендация:** Начнем с **YouTube Music через yt-dlp** - самый простой и надежный вариант.

### Хранилище

- **SQLite** (опционально для кэша)
  - Хранение результатов поиска
  - Статистика использования
  - Популярные треки

---

## 📁 Структура проекта

```
UspMusicFinder/
├── src/
│   ├── bot.py              # Инициализация бота
│   ├── config.py           # Настройки из .env
│   ├── main.py             # Точка входа
│   │
│   ├── handlers/
│   │   ├── start.py        # /start, /help
│   │   ├── search.py       # Поиск по тексту
│   │   ├── top.py          # TOP популярных
│   │   ├── inline.py       # Inline режим
│   │   └── callbacks.py    # Обработка кнопок (1-10)
│   │
│   ├── searchers/
│   │   ├── base.py         # Базовый класс поисковика
│   │   ├── youtube.py      # YouTube Music поиск
│   │   ├── itunes.py       # iTunes API
│   │   └── vk.py           # VK Music (опционально)
│   │
│   ├── downloaders/
│   │   ├── youtube_dl.py   # Скачивание через yt-dlp
│   │   └── direct_dl.py    # Прямые ссылки
│   │
│   └── utils/
│       ├── logger.py       # Логирование
│       ├── cache.py        # Кэширование результатов
│       └── metadata.py     # Работа с метаданными (mutagen)
│
├── data/
│   ├── temp/               # Временные файлы
│   ├── cache/              # Кэш результатов
│   └── database.db         # SQLite (опционально)
│
├── logs/                   # Логи
├── .env                    # Конфигурация
└── requirements.txt        # Зависимости
```

---

## 📝 Основные функции

### 1. Поиск музыки

**Входные данные:**
- Текстовый запрос от пользователя (например: "Время назад")

**Процесс:**
1. Получить запрос
2. Искать в YouTube Music через yt-dlp: `ytsearch10:Время назад`
3. Получить список до 10 результатов:
   - Название трека
   - Исполнитель
   - Длительность
   - Ссылка
4. Сохранить результаты в кэш с ID пользователя
5. Создать inline keyboard с кнопками 1-10
6. Отправить пользователю

**Пример кода (псевдокод):**
```python
from yt_dlp import YoutubeDL

async def search_music(query: str) -> List[Track]:
    ydl_opts = {
        'quiet': True,
        'no_warnings': True,
        'extract_flat': True,
    }

    with YoutubeDL(ydl_opts) as ydl:
        result = ydl.extract_info(f"ytsearch10:{query}", download=False)
        entries = result.get('entries', [])

        tracks = []
        for i, entry in enumerate(entries, 1):
            track = Track(
                id=entry['id'],
                title=entry['title'],
                duration=entry.get('duration', 0),
                url=entry['url']
            )
            tracks.append(track)

        return tracks
```

### 2. Скачивание трека

**Входные данные:**
- YouTube URL или video ID

**Процесс:**
1. Скачать аудио через yt-dlp в MP3 формат
2. Сохранить в `data/temp/`
3. Извлечь метаданные (название, исполнитель)
4. Добавить теги ID3 (mutagen)
5. Вернуть путь к файлу

**Пример кода:**
```python
async def download_track(video_id: str) -> str:
    output_template = 'data/temp/%(id)s.%(ext)s'

    ydl_opts = {
        'format': 'bestaudio/best',
        'outtmpl': output_template,
        'postprocessors': [{
            'key': 'FFmpegExtractAudio',
            'preferredcodec': 'mp3',
            'preferredquality': '192',
        }],
    }

    with YoutubeDL(ydl_opts) as ydl:
        info = ydl.extract_info(f"https://youtube.com/watch?v={video_id}", download=True)
        filename = ydl.prepare_filename(info).replace('.webm', '.mp3').replace('.m4a', '.mp3')

    return filename
```

### 3. Отправка в Telegram

**Входные данные:**
- Путь к MP3 файлу
- Метаданные (название, исполнитель, длительность)

**Процесс:**
1. Загрузить файл как `InputFile`
2. Отправить через `bot.send_audio()`
3. Указать `performer`, `title`, `duration`
4. Удалить временный файл

**Пример кода:**
```python
async def send_audio_to_user(chat_id: int, file_path: str, metadata: dict):
    audio = FSInputFile(file_path)

    await bot.send_audio(
        chat_id=chat_id,
        audio=audio,
        performer=metadata.get('artist', 'Unknown'),
        title=metadata.get('title', 'Unknown'),
        duration=metadata.get('duration', 0)
    )

    # Удалить файл после отправки
    os.remove(file_path)
```

---

## 🎛️ Handlers (обработчики)

### 1. /start и /help

```python
@router.message(Command("start"))
async def cmd_start(message: Message):
    await message.answer(
        "Привет! Чтобы я смог распознать музыку, отправь мне что-то из этого:\n\n"
        "• Название песни или исполнителя\n"
        "• Слова из песни\n"
        "• Голосовое сообщение\n"
        "• Видео\n"
        "• Аудио\n"
        "• Видеосообщение\n\n"
        "(Бот работает напрямую и в чатах тоже.)"
    )
```

### 2. Поиск по тексту

```python
@router.message(F.text)
async def search_handler(message: Message, state: FSMContext):
    query = message.text

    # Поиск треков
    tracks = await search_music(query)

    if not tracks:
        await message.answer("Ничего не найдено. Попробуй другой запрос.")
        return

    # Сохранить в кэш для пользователя
    await cache.set(f"search:{message.from_user.id}", tracks, ttl=600)

    # Создать кнопки
    keyboard = create_track_keyboard(tracks)

    # Формировать список треков
    text = f"{query}\n\n"
    for i, track in enumerate(tracks, 1):
        duration = format_duration(track.duration)
        text += f"{i}. {track.title} {duration}\n"

    await message.answer(text, reply_markup=keyboard)
```

### 3. Callback обработчик (кнопки 1-10)

```python
@router.callback_query(F.data.startswith("track:"))
async def track_callback(callback: CallbackQuery):
    # Получить номер трека
    track_num = int(callback.data.split(":")[1])

    # Получить из кэша список треков
    tracks = await cache.get(f"search:{callback.from_user.id}")

    if not tracks:
        await callback.answer("Результаты поиска устарели. Поищи заново.")
        return

    track = tracks[track_num - 1]

    # Показать "Загрузка..."
    await callback.message.edit_text("Загрузка...")

    # Скачать трек
    file_path = await download_track(track.id)

    # Отправить
    await send_audio_to_user(
        callback.from_user.id,
        file_path,
        {'artist': track.artist, 'title': track.title, 'duration': track.duration}
    )

    await callback.answer()
```

### 4. TOP популярных

```python
@router.message(Command("top"))
async def cmd_top(message: Message):
    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [
            InlineKeyboardButton(text="🇺🇿", callback_data="top:uz"),
            InlineKeyboardButton(text="🇷🇺", callback_data="top:ru"),
            InlineKeyboardButton(text="🇬🇧", callback_data="top:en"),
            InlineKeyboardButton(text="🇰🇿", callback_data="top:kz"),
            InlineKeyboardButton(text="🇹🇷", callback_data="top:tr"),
            InlineKeyboardButton(text="🇦🇿", callback_data="top:az"),
        ]
    ])

    await message.answer("TOP Popular Songs\n\nChoose a language", reply_markup=keyboard)

@router.callback_query(F.data.startswith("top:"))
async def top_callback(callback: CallbackQuery):
    country = callback.data.split(":")[1]

    # Получить топ песен для страны
    top_tracks = await get_top_tracks(country)

    # Показать список
    keyboard = create_track_keyboard(top_tracks)

    text = "TOP Popular Songs\n\n"
    for i, track in enumerate(top_tracks, 1):
        text += f"{track.artist} — {track.title}\n"

    await callback.message.edit_text(text, reply_markup=keyboard)
```

### 5. Inline режим

```python
@router.inline_query()
async def inline_search(inline_query: InlineQuery):
    query = inline_query.query

    if not query:
        return

    # Поиск треков
    tracks = await search_music(query)

    # Создать результаты
    results = []
    for i, track in enumerate(tracks[:10]):
        result = InlineQueryResultAudio(
            id=str(i),
            audio_url=track.url,
            title=track.title,
            performer=track.artist,
            audio_duration=track.duration
        )
        results.append(result)

    await inline_query.answer(results, cache_time=300)
```

---

## 🔐 Безопасность

### 1. Защита токена
- API Token хранится в `.env`
- `.env` в `.gitignore`

### 2. Ограничения
- Максимальный размер файла: **50 MB** (лимит Telegram)
- Максимальная длительность: **10 минут**
- Rate limiting: не более 5 запросов в минуту от одного пользователя

### 3. Обработка ошибок
- Try/except для всех API запросов
- Логирование ошибок
- Уведомление пользователя о проблемах

---

## 📊 Кэширование и оптимизация

### Кэш результатов поиска

```python
# Кэш в памяти (Redis-like)
cache = {
    "search:{user_id}": {
        "data": [...],  # Список треков
        "ttl": 600       # 10 минут
    }
}
```

### Кэш популярных треков

- TOP треки кэшируются на 1 час
- Обновление раз в час из API

### Оптимизация загрузки

- Параллельная загрузка метаданных
- Использование `asyncio.gather()` для множественных запросов

---

## 📈 Мониторинг и статистика

### Логирование

```python
# logs/bot.log
2025-12-04 08:18:00 | INFO | User 12345 searched: "Время назад"
2025-12-04 08:18:05 | INFO | Downloading track: dQw4w9WgXcQ
2025-12-04 08:18:10 | INFO | Sent audio to user 12345
```

### Статистика (опционально)

- Количество поисков
- Популярные запросы
- Количество скачанных треков
- Активные пользователи

```sql
CREATE TABLE stats (
    id INTEGER PRIMARY KEY,
    user_id INTEGER,
    query TEXT,
    track_id TEXT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 🚀 План разработки (4 недели)

### Неделя 1: MVP - Базовый поиск и скачивание

**День 1-2: Настройка и базовый бот**
- [x] Создать проект
- [ ] Настроить окружение
- [ ] Базовый бот с `/start` и `/help`
- [ ] Логирование

**День 3-4: YouTube Music поиск**
- [ ] Интеграция yt-dlp
- [ ] Функция поиска `search_music()`
- [ ] Парсинг результатов
- [ ] Тесты

**День 5-7: Скачивание и отправка**
- [ ] Функция `download_track()`
- [ ] Конвертация в MP3
- [ ] Метаданные (mutagen)
- [ ] Отправка в Telegram
- [ ] Удаление временных файлов

**Результат Week 1:** Бот ищет и скачивает музыку по тексту

---

### Неделя 2: Улучшения и UI

**День 8-10: Inline keyboard**
- [ ] Создание кнопок 1-10
- [ ] Callback handlers
- [ ] Кэширование результатов поиска
- [ ] Обработка истекших результатов

**День 11-12: TOP популярных**
- [ ] `/top` команда
- [ ] Кнопки выбора страны (флаги)
- [ ] API для TOP треков (Chart API или hardcoded список)
- [ ] Кэш топов на 1 час

**День 13-14: Полировка**
- [ ] Красивые сообщения
- [ ] Прогресс индикаторы ("Загрузка...")
- [ ] Обработка ошибок (не найдено, ошибка скачивания)
- [ ] Тесты

**Результат Week 2:** Полноценный UI с кнопками и TOP

---

### Неделя 3: Inline режим и альтернативные источники

**День 15-17: Inline режим**
- [ ] `@UspMusicFinder_bot` inline query handler
- [ ] `InlineQueryResultAudio`
- [ ] Кэш inline результатов
- [ ] Тесты в разных чатах

**День 18-19: Альтернативные источники**
- [ ] iTunes API поиск (опционально)
- [ ] Deezer API (опционально)
- [ ] VK Music (опционально)
- [ ] Fallback механизм (если YouTube не нашел)

**День 20-21: Расширенные функции**
- [ ] Распознавание голосовых сообщений (Shazam API / AudD)
- [ ] Поиск по аудио/видео
- [ ] Lyrics (текст песни) - опционально

**Результат Week 3:** Inline режим + расширенный функционал

---

### Неделя 4: Production Ready

**День 22-24: Оптимизация**
- [ ] Rate limiting
- [ ] Кэш в Redis (опционально)
- [ ] Статистика в SQLite
- [ ] Очистка старых файлов

**День 25-26: Деплой**
- [ ] Dockerfile
- [ ] docker-compose.yml
- [ ] Деплой на VPS
- [ ] Systemd service
- [ ] Nginx (если нужен webhook)

**День 27-28: Мониторинг**
- [ ] Healthcheck endpoint
- [ ] Логи в файл + ротация
- [ ] Уведомления админу о критических ошибках
- [ ] Backup базы данных

**Результат Week 4:** Production-ready бот на VPS

---

## 🛠️ Технические детали

### yt-dlp параметры для поиска

```python
ydl_opts = {
    'format': 'bestaudio/best',
    'outtmpl': 'data/temp/%(id)s.%(ext)s',
    'quiet': True,
    'no_warnings': True,
    'extract_flat': False,  # Получить детали
    'postprocessors': [{
        'key': 'FFmpegExtractAudio',
        'preferredcodec': 'mp3',
        'preferredquality': '192',
    }],
}

# Поиск
result = ydl.extract_info(f"ytsearch10:Время назад", download=False)

# Скачивание
result = ydl.extract_info(f"https://youtube.com/watch?v={video_id}", download=True)
```

### Метаданные с mutagen

```python
from mutagen.easyid3 import EasyID3
from mutagen.mp3 import MP3

# Добавить теги
audio = EasyID3(file_path)
audio['title'] = track_title
audio['artist'] = artist_name
audio['album'] = album_name
audio.save()

# Получить длительность
audio = MP3(file_path)
duration = int(audio.info.length)  # в секундах
```

### Форматирование длительности

```python
def format_duration(seconds: int) -> str:
    minutes = seconds // 60
    secs = seconds % 60
    return f"{minutes}:{secs:02d}"

# Пример: 253 → "4:13"
```

---

## 🌍 Источники TOP треков

### Варианты API:

1. **Last.fm API** (бесплатный)
   - `https://ws.audioscrobbler.com/2.0/?method=geo.gettoptracks&country=russia`

2. **Spotify Charts** (web scraping)
   - `https://charts.spotify.com/charts/view/regional-{country}-weekly/latest`

3. **YouTube Music Charts** (через yt-dlp)
   - Плейлист "Top 100" для разных стран

4. **Hardcoded список** (временное решение)
   - Вручную составленный список популярных треков

**Рекомендация:** Начать с **Last.fm API**

---

## 📦 requirements.txt

```txt
# Telegram Bot
aiogram==3.15.0
aiohttp==3.11.11
aiofiles==24.1.0

# Downloaders
yt-dlp==2024.12.13

# Audio processing
mutagen==1.47.0

# Core
python-dotenv==1.0.1
requests==2.32.3

# Cache (optional)
redis==5.2.1

# Database (optional)
aiosqlite==0.20.0

# Dev dependencies
pytest==8.3.4
pytest-asyncio==0.24.0
black==24.10.0
```

---

## 🔧 .env конфигурация

```env
# Telegram Bot
BOT_TOKEN=8409655187:AAHzhBh8F9KuMiAyemy-IVlF3yNgXXpPI6E
BOT_USERNAME=UspMusicFinder_bot

# Paths
TEMP_DIR=./data/temp
CACHE_DIR=./data/cache
LOGS_DIR=./logs
DATABASE_PATH=./data/database.db

# Limits
MAX_FILE_SIZE=52428800  # 50MB
MAX_DURATION=600  # 10 minutes
RATE_LIMIT_REQUESTS=5  # per minute
RATE_LIMIT_PERIOD=60  # seconds

# Logging
LOG_LEVEL=INFO

# Features
ENABLE_CACHE=true
ENABLE_STATS=true
ENABLE_INLINE=true

# APIs (optional)
LASTFM_API_KEY=
AUDD_API_KEY=  # для распознавания музыки
```

---

## 🎨 Примеры сообщений и UI

### Приветствие (/start)

```
Привет! Чтобы я смог распознать музыку, отправь мне что-то из этого:

• Название песни или исполнителя
• Слова из песни
• Голосовое сообщение
• Видео
• Аудио
• Видеосообщение

(Бот работает напрямую и в чатах тоже.)
```

### Результаты поиска

```
Время назад

1. Время, назад! 4:13
2. Сплин «Время, назад» 4:27
3. Из фильма Егерь песня Сплин Время назад .avi 2:18
4. Пикник - Азбука Морзе (любительский клип) 4:18
5. Ольга Арефьева и Ковчег "Время, назад!" 7:30
6. Дени Сатабаев - Время назад 2:22
7. Сплин - Время назад (revision '2023) 4:35
8. Ула и Жако - Время назад 3:21
9. Время назад 2:56
10. Руслан Добрый - Пару минут 2:39

[Кнопки: 1 2 3 4 5]
[Кнопки: 6 7 8 9 10]
```

### TOP Popular Songs

```
TOP Popular Songs

Choose a language

[🇺🇿] [🇷🇺] [🇬🇧] [🇰🇿] [🇹🇷] [🇦🇿]
```

После выбора страны:

```
TOP Popular Songs

Jakone & Kiliana — Жиганская
Icegergert & Zivert — БАНК
NEXTIME — СВЕТЛАНА!
KDDK & Alex Alta — 1&2
Albert Brite — Wild
Ваня Дмитриенко & Аня Пересильд — С...
Aarne, Toxi$ & Big Baby Tape — NOBODY
Inna — Caliente (Radio Edit)
ELMAN — С неба (feat. TRIDA)
Линочка Ли — Любить тебя

[Кнопки для выбора трека]
```

---

## 🚨 Обработка ошибок

### 1. Ничего не найдено

```python
if not tracks:
    await message.answer("❌ Ничего не найдено. Попробуй другой запрос.")
```

### 2. Ошибка скачивания

```python
try:
    file_path = await download_track(track_id)
except Exception as e:
    logger.error(f"Download error: {e}")
    await message.answer("❌ Ошибка при скачивании. Попробуй другой трек.")
```

### 3. Файл слишком большой

```python
file_size = os.path.getsize(file_path)
if file_size > MAX_FILE_SIZE:
    await message.answer("❌ Файл слишком большой (>50MB). Telegram не поддерживает.")
    os.remove(file_path)
```

### 4. Rate limit превышен

```python
if await is_rate_limited(user_id):
    await message.answer("⏳ Слишком много запросов. Подожди минутку.")
    return
```

---

## 📱 Inline режим детали

### Настройка бота

В @BotFather:
```
/setinline
@UspMusicFinder_bot
Введи название песни...
```

### Пример использования

В любом чате:
```
@UspMusicFinder_bot Время назад
```

Бот показывает результаты inline, пользователь выбирает трек и он отправляется в чат.

---

## 🎓 Альтернативные фичи (опционально)

### 1. Распознавание музыки из аудио

**API:** AudD.io или Shazam API

```python
@router.message(F.audio)
async def recognize_audio(message: Message):
    # Скачать аудио
    file = await bot.get_file(message.audio.file_id)
    file_path = f"data/temp/{message.audio.file_id}.mp3"
    await bot.download_file(file.file_path, file_path)

    # Распознать через API
    track_info = await recognize_track(file_path)

    if track_info:
        await message.answer(f"Это: {track_info['artist']} - {track_info['title']}")
    else:
        await message.answer("Не удалось распознать трек.")
```

### 2. Текст песни (Lyrics)

**API:** Genius API, Musixmatch

```python
@router.message(Command("lyrics"))
async def get_lyrics(message: Message):
    # Получить последний скачанный трек пользователя
    track = await get_last_track(message.from_user.id)

    lyrics = await fetch_lyrics(track.artist, track.title)

    if lyrics:
        await message.answer(lyrics[:4000])  # Telegram лимит
    else:
        await message.answer("Текст песни не найден.")
```

### 3. Плейлисты

Сохранять избранные треки пользователя в базу данных.

```sql
CREATE TABLE playlists (
    user_id INTEGER,
    track_id TEXT,
    track_title TEXT,
    artist TEXT,
    added_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 📊 База данных (SQLite) - опционально

```sql
-- Статистика поисков
CREATE TABLE search_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    query TEXT NOT NULL,
    results_count INTEGER,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Статистика скачиваний
CREATE TABLE download_stats (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    track_id TEXT NOT NULL,
    track_title TEXT,
    artist TEXT,
    timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- Популярные запросы
CREATE VIEW popular_queries AS
SELECT query, COUNT(*) as count
FROM search_stats
GROUP BY query
ORDER BY count DESC
LIMIT 100;
```

---

## 🔗 Полезные ссылки

### APIs

- **YouTube Music:** через yt-dlp
- **iTunes Search API:** https://developer.apple.com/library/archive/documentation/AudioVideo/Conceptual/iTuneSearchAPI/
- **Deezer API:** https://developers.deezer.com/api
- **Last.fm API:** https://www.last.fm/api
- **AudD API (распознавание):** https://audd.io/

### Библиотеки

- **aiogram:** https://docs.aiogram.dev/
- **yt-dlp:** https://github.com/yt-dlp/yt-dlp
- **mutagen:** https://mutagen.readthedocs.io/

---

## ✅ Checklist для MVP

- [ ] Базовый бот (start, help)
- [ ] Поиск музыки по тексту
- [ ] Скачивание MP3
- [ ] Отправка аудио в Telegram
- [ ] Inline keyboard с кнопками 1-10
- [ ] Callback handlers
- [ ] Кэширование результатов
- [ ] Обработка ошибок
- [ ] Логирование
- [ ] TOP популярных (базовая версия)

---

**Проект готов к разработке!** 🚀

Следующий шаг: Создать [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md) с детальным планом на 4 недели.
